[project]
name = "dlms-shipment-sample"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=48.0.0",
    "lxml",
]

[dependency-groups]
dev = ["pytest", "ruff"]

[tool.pytest.ini_options]
pythonpath = ["."]
